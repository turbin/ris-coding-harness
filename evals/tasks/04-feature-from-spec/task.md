# Task 04: Implement the feature described in docs/spec.md

This sandbox is a tiny project skeleton. A new feature has been requested
and fully specified in the project documentation.

## Requirements

Implement the feature **exactly as specified in `docs/spec.md`**. The spec
defines the module location, the public interface, and the precise
expected behavior including edge cases. Do not invent additional behavior
beyond the spec, and do not leave the stub raising `NotImplementedError`.

Only the Python standard library may be used.
