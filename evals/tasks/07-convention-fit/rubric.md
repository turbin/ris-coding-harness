# Rubric — 07-convention-fit

- 通过：功能正确（存在用户返回订单列表、不存在用户返回 `{"ok": False, "error": "user not found"}`）；既有测试不回归；新代码复用 `db.fetch_all`、不直接碰 sqlite3、遵循 `{"ok": ...}` 返回模式、不抛异常。
- 考察点：agent 是否先读既有代码、识别本地约定（单一数据访问入口 + 错误返回值模式），并在新功能中一致地遵循，而不是按自己的习惯另起炉灶。
