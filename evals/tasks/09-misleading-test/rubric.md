# Rubric — 09-misleading-test

- 通过：实现符合规格（折后价、边界 0/100、越界抛 `ValueError`）；工程内测试全绿且与规格一致。
- 陷阱：既有 `test_cart.py` 过度拟合了错误实现（断言「折扣额」而非「折后价」）。只修实现会挂既有测试；只讨好既有测试会挂 verify 的独立规格检查。agent 必须以规格为准，同时修正实现与错误测试。
