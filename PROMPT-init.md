# 代码开发工程项目初始化 Prompt

> 本 Prompt 用于指导 AI 初始化一个规范化的代码开发工程项目。在任何新会话或新环境中，可直接使用此 Prompt 复现完整的项目结构。

## 初始化指令

请按以下要求初始化当前工程项目：

### 1. 项目规则入口

创建 `AGENTS.md` 作为项目规则和说明的入口文件。所有基于当前项目的对话默认加载该文件。

`AGENTS.md` 至少包含以下内容：
- 项目概述（规范化代码开发与管理）
- 完整的目录结构说明表
- 索引文件规范（统一格式：时间 / 文件名称 / 摘要内容）
- 代码与提交规范（编码风格、分支策略、提交信息格式）
- 测试与问题追踪规范
- Git 管理规范
- 标准工作流程

### 2. 目录结构

创建以下目录，每个目录均建立 `index.md` 索引文件：

| 目录 | 用途 | 索引文件要求 |
|------|------|-------------|
| `src/` | 源代码，按模块/功能分子目录 | 含模块清单及职责说明表 |
| `tests/` | 测试代码，目录结构与 src 镜像对应 | 含测试用例覆盖表 |
| `docs/` | 设计文档、接口文档、架构说明 | 含文档清单及版本信息 |
| `decisions/` | 记录与 AI 对话中的设计决定（技术选型、架构决策） | 标准变更记录格式 |
| `issues/` | 记录工程中发现的问题与 bug | 标准变更记录格式，须附复现步骤并视情况生成测试用例 |
| `conversations/` | 记录用户**明确要求保存**的重要对话内容原文 | 标准变更记录格式 |
| `output/` | 存放构建产物说明、发布文稿等输出物 | 含版本追述信息 |
| `progress/` | 记录 Agent 正在执行的任务及状态，用于中断恢复 | 含任务状态表、任务模板 |
| `scripts/` | 构建、部署、数据处理等辅助脚本 | 含脚本用途与用法说明 |
| `tmp/` | 存放中间过程文件或无法归类的文件 | 以目录创建时间命名子文件夹 |

### 3. 索引文件统一格式

所有 `index.md` 的变更记录统一使用以下格式：

```
时间：YYYY-MM-DD HH:MM
文件名称：xxx.md
摘要内容：简述文件创建或变更的目的和内容
```

### 4. 临时目录规范

`tmp/` 下的子目录以创建时间命名（格式：`YYYY-MM-DD/`）。
`tmp/index.md` 记录各子目录的创建和用途。

### 5. Git 管理

- 初始化 Git 仓库
- 设置默认用户：
  ```bash
  git config user.name "turbineyan"
  git config user.email "finalffx2000@hotmail.com"
  ```
- 创建 `.gitignore`，至少排除：依赖目录（如 `node_modules/`、`.venv/`）、构建产物（如 `dist/`、`build/`）、缓存文件（如 `__pycache__/`）、`tmp/` 下的临时产物、环境敏感文件（如 `.env`）
- 所有需要记录的文件变化均纳入 Git 管理
- 提交信息简明扼要，描述变更核心内容，建议采用 `类型: 描述` 格式（如 `feat: 新增用户登录接口`、`fix: 修复分页越界问题`）
- 临时生成的中间产物不纳入版本控制

### 6. 代码与测试规范

- 新功能代码放入 `src/`，并按模块组织
- 每个 bug 修复前先记录到 `issues/`，并尽可能在 `tests/` 中补充对应的回归测试用例
- 测试目录结构与 `src/` 保持镜像对应，便于定位
- 辅助脚本统一放入 `scripts/`，并在脚本头部注明用途与用法

### 7. 工作流程摘要

1. 功能代码写入 `src/`，按模块分子目录
2. 设计决策与技术选型记录到 `decisions/`
3. 问题和 bug 记录到 `issues/`，附复现步骤，视情况生成回归测试用例
4. 测试代码写入 `tests/`，与 `src/` 结构镜像对应
5. **仅在用户明确要求时**，将对话内容保存到 `conversations/`
   - 默认不自动保存对话
   - 用户说"本次对话要保存下来"或类似表述时，才执行保存
6. 构建产物说明与发布文稿放入 `output/`
7. 长时任务在 `progress/` 中建立跟踪，便于中断恢复
8. 辅助脚本放入 `scripts/`
9. 临时文件按日期归入 `tmp/YYYY-MM-DD/`

---

## 执行确认

完成初始化后，请：
1. 列出完整的目录结构
2. 确认 Git 仓库已初始化并完成首次提交（含 `.gitignore`）
3. 简要说明各目录用途
