# Environment agent configuration — sourced by run-loop.sh and referenced by
# skills/rsi-loop/references/preflight.md (checklist item 6).
#
# 2026-08-29: on this machine only pi and kimi are usable (user-confirmed);
# claude / opencode / codex are installed but not usable here. Keep this list
# in sync with the actual environment when it changes.
RSI_AGENT_CANDIDATES="pi kimi"
