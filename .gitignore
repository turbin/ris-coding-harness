# RSI eval sandboxes (regenerated by evals/run-eval.sh setup)
evals/sandbox/

# macOS junk
.DS_Store

# eval event-trigger marker (git hooks set it; evals/trigger.sh consumes it)
evals/.eval-pending
evals/.commit-count
